
{
  "name": "rentmaster-pro",
  "version": "1.0.0",
  "description": "Rental Management Engine with Google Sheets & Vercel Hosting",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "build": "mkdir -p public && cp index.html index.tsx types.ts App.tsx public/ && cp -r components store public/ 2>/dev/null || true",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.0",
    "lucide-react": "^0.460.0",
    "recharts": "^2.13.0"
  }
}
